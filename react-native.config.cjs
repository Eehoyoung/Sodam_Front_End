// React Native 0.81+ uses the Gradle/Pods autolinking system.
// Manual platform path overrides can break native module resolution under New Architecture.
// Keep this file minimal; only declare project-level options or assets if needed.

module.exports = {
    project: {
        android: {},
        ios: {},
    },
    // If you have custom fonts or assets to link, add them here, e.g.:
    // assets: ['./src/assets/fonts'],
};
