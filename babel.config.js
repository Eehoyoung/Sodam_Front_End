module.exports = {
  // Use the official React Native preset with automatic JSX runtime defaults
  presets: ['@react-native/babel-preset'],
  // Reanimated plugin must be listed last
  plugins: ['react-native-reanimated/plugin'],
};
